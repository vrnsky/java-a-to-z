package service;

/**
 * Model of user storage. Hold object only user type
 */
public class UserStorage extends AbstractStorage<User> {

    /**
     * Create a new user storage.
     */
    public UserStorage() {
        super();
    }
}
